void setup() {
  // put your setup code here, to run once:

  // setup joystick potentiometer for left/right
  // setup joystick potentiometer for forward/back

  // setup for radio transmitter

}

void loop() {
  // put your main code here, to run repeatedly:

  // check left/right value
  // check forward/back value

  // broadcast left/right value
  // broadcast forward/back value

  // wait some interval
}
